[pytest]
testpaths = tests
markers =
    integration: optional tests that require external services such as a test PostgreSQL database
    real_llm_online: optional tests that send requests to an explicitly configured OpenAI-compatible LLM
