# AGENTS.md

本文件是给 Codex / AI Coding Agent 的项目级开发规则。每次开发前必须阅读并遵守。

---

## 1. 项目背景

当前项目是“AI 教学智能体（独立版）”。

目标包括：

1. AI 生成教学实验。
2. AI 将旧实验改造成考试 / 竞赛试题。
3. AI 生成自动评分脚本。
4. 对选手提交进行机器自动判分。
5. 管理 VM / Notebook 实验环境。
6. 智能体能力 CLI 化，供大模型或外部系统调用。
7. 封装 MCP Tools，构建可独立运行的智能体服务。
8. 建设独立智能体前端交互页面。
9. 沉淀 Skills、Prompt、Workflow，供智能体复用与编排。

定位：独立运行的智能体，不再以接入外部实训平台为核心目标。

---

## 2. 最高优先级原则

先工具化，再智能体化。

正确顺序：

```text
DSL → CLI → Mock → Workflow → Sandbox → MCP → Agent → Operation
```

禁止一开始直接开发复杂 Agent。

### 2.1 阶段封口与防循环规则

当前项目已经完成多轮 Mock、Provider、SDK 边界和 request-send 安全门禁建模。后续不得继续无上限追加“安装前禁用壳”“请求发送禁用壳”“最终执行禁用壳”或同义安全壳。

明确封口点：

```text
real-llm-request-send-attempt-gate-disabled
```

完成该点后，下一步默认进入：

```text
真实 SDK 安装执行 → 环境变量边界 → SDK import/client 边界 → 最小真实 LLM 单请求 PoC → 核心业务开发
```

除非用户明确要求新增某个安全壳，并说明它要防止的具体风险，否则 Codex 不得继续创建新的 pre-install / request-send / executor / authorization / gate disabled 模块。

每次输出“下一步建议”时，必须包含：

```text
建议智能模式：高智能模式 / 超高智能模式
```

当下一步涉及真实 SDK 安装、真实依赖解析、真实环境变量读取、SDK import、client 构造或真实 LLM 请求时，默认建议使用“超高智能模式”。

### 2.2 运营内容暂停与核心功能优先规则

当前运营交付、运营验收、`operations-*` 静态页、`delivery/` 运营手册和运营签收内容先暂停扩展。已有内容只做收尾、勘误和归档说明，后续默认不得新增运营页面、运营验收清单、运营脚本、运营交付包或运营向 Skills / Prompt 文档，除非用户明确恢复运营交付任务。

后续默认优先：

1. 真实 LLM Lab / Exam / Grading / PPT 生成质量与稳定性。
2. DSL Schema、真实输出归一化与 Schema 校验通过率。
3. 审核详情、导入预览、签收清单只服务智能体内部内容闭环。
4. Grading DSL、自动评分计划与受控沙箱。
5. MCP Tools 只封装智能体核心能力。
6. 智能体内部实体的本地持久化、版本管理与状态流转。

输出“下一步建议”时，不再建议外部平台对接、运营交付、运营验收或 `operations-*` 页面开发，默认建议智能体核心业务功能。

### 2.3 独立智能体内部闭环与数据管理规则

当前项目定位为独立智能体，其核心是构建不依赖外部平台的内部数据流转闭环。

当前本地闭环核心链路是：

```text
真实 LLM 生成 DSL → Schema 校验 → WAITING_REVIEW/APPROVED 审核任务
→ 候选人安全预览 → 受控 Docker 评分 evidence → review decision note
→ 本地实体持久化 → 状态管理与版本记录 → 对外服务或 API 输出
```

Codex 应专注于完善智能体自身的能力，包括：
1. **实体管理**：确保生成的 Lab/Exam/Grading 等实体在智能体内部有明确的存储和状态。
2. **核心服务**：完善 MCP Server、CLI 工具、评分沙箱等基础设施，使其成为可独立调用的原子能力。
3. **前端交互**：建设独立的智能体操作界面（如对话式界面、生成面板等），而非单纯的数据管理页面。

不再要求或提及外部实训平台 API base URL、平台 token、字段映射等对接细节。智能体的目标是通过其自身的工具链和 MCP 协议对外提供服务，而不是作为外部平台的附属模块。

### 2.4 项目进度地图强制规则

`docs/24_PROJECT_PROGRESS_MAP.md` 是当前项目的进度地图、未完成事项清单、复杂度评估和防跑偏停止线。后续 Codex / AI Coding Agent 必须把它作为项目级执行规则，而不是普通参考文档。

每次继续开发前必须读取并遵守：

```text
docs/24_PROJECT_PROGRESS_MAP.md
```

执行要求：

1. 先判断当前任务是否属于该文档的 P0 / P1 / P2 / P3 路线。
2. 优先推进该文档的 P0/P1 核心业务任务，除非用户明确指定其他任务。
3. 当某个功能达到该文档中“做到什么就停”的停止线后，必须切换到下一项，不得继续在同一功能点追加同义文档、门禁、禁用壳、展示页或验收壳。
4. 若下一步建议与 `docs/24_PROJECT_PROGRESS_MAP.md` 冲突，必须以该文档为准，并说明冲突原因。
5. 不得因为历史 README 或旧 Phase 文档中存在大量安全壳记录，就继续沿用旧路线绕圈。

---

## 3. 当前阶段默认限制

除非用户明确说明，否则默认处于 Phase 1。

当前项目已经在用户明确要求下进入真实 LLM Demo MVP 和核心业务开发阶段。对于后续“继续开发”“完善核心功能”“真实大模型产出成果”“演示闭环”等任务，应以 `docs/24_PROJECT_PROGRESS_MAP.md` 的当前定位和路线为准，不得退回 Phase 1 Mock 规划循环。

Phase 1 只允许：

- 创建目录
- 编写文档
- 定义 DSL Schema
- 创建示例
- 实现 CLI Mock
- 实现 AI Task Mock
- 实现人工审核状态流转
- 编写测试

Phase 1 禁止：

- 接入真实大模型
- 接入真实云资源
- 对外发布或执行真实任务
- 执行未知 Shell 脚本
- 无沙箱执行选手代码

---

## 4. 开发前必须做

每次接到任务后：

1. 阅读本文件。
2. 阅读 `docs/AI_PLATFORM_CODEX_FULL_GUIDE.md`。
3. 判断任务属于哪个 Phase 和模块。
4. 检查现有实现，避免重复造轮子。
5. 给出简短执行计划。
6. 只改动与任务相关文件。

---

## 5. 开发后必须做

每次完成后输出：

```text
本次完成：
- ...

修改文件：
- ...

如何验证：
- ...

测试结果：
- ...

风险与注意事项：
- ...

下一步建议：
- ...
```

---

## 6. 工程约定

### 6.1 DSL 优先

AI 生成内容必须先输出 DSL，再转换成智能体可操作的内部实体或直接通过 API 对外提供服务。

必须支持：

- Lab DSL
- Exam DSL
- Grading DSL
- PPT DSL

### 6.2 CLI 返回 JSON

所有 CLI 命令必须返回统一 JSON。

### 6.3 AI 输出必须审核

AI 生成实验、试题、评分规则、PPT、验收文档后，默认状态为：

```text
WAITING_REVIEW
```

审核通过前不得发布。

### 6.4 Prompt 不得散落在业务代码

Prompt 必须放在：

```text
prompts/
skills/
```

业务代码只引用 prompt 文件。

### 6.5 密钥不得写死

API Key、Token、密码必须来自环境变量、配置中心或本地 `.env.example` 示例，不得进入 Git。

---

## 7. 安全红线

禁止：

1. 直接操作生产数据库。
2. 直接删除云资源。
3. 直接执行未知 Shell 脚本。
4. 无沙箱执行选手代码。
5. 将 AI 生成结果自动发布。
6. 在日志中输出密钥。
7. 把标准答案展示给选手端。
8. 绕过人工审核。
9. 随意修改已有公共接口。
10. 一次性大范围重构。

---

## 8. 测试要求

每个模块至少包含：

1. 正常路径测试。
2. 参数错误测试。
3. Schema 校验失败测试。
4. 状态非法流转测试。
5. CLI 返回格式测试。

---

## 9. 文档要求

修改代码时必须同步更新相关 README 或 docs。

新增模块必须包含：

```text
README.md
输入说明
输出说明
命令示例
测试方式
限制说明
```

---

## 10. 完成定义

一个任务只有满足以下条件才算完成：

1. 功能实现。
2. 有示例输入。
3. 有示例输出。
4. 有测试。
5. 有错误处理。
6. 有 README。
7. 可通过 CLI 或测试验证。
8. 未违反安全红线。
