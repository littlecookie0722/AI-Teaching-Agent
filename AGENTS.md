# AGENTS.md

本文件是当前项目的唯一“逐回合执行约束”。它负责说明 Codex 如何工作、哪些事情不能做，以及当前范围如何收敛。

## 0. 规则优先级与文档分工

发生冲突时按以下顺序处理：

1. 用户当前明确任务（仍不得违反安全红线）。
2. 本文件：全局执行方式、禁止事项和交付要求。
3. `docs/24_PROJECT_PROGRESS_MAP.md`：当前路线、优先级和功能停止线。
4. 被修改模块的契约/Schema/API 文档：该模块的输入、输出和兼容性要求。
5. 其他 README、Phase 文档和完整指南：架构背景或历史参考，不覆盖以上规则。

因此：

- 每次开发必须读本文件和进度地图中与任务相关的部分；不要求每轮全文重读历史指南。
- 当前状态以 `docs/24_PROJECT_PROGRESS_MAP.md` 为准；旧 Phase 文档中的“下一步”不自动代表当前下一步。
- 新增规则前先判断是否已有上位规则可以表达它；同一风险只保留一个主动约束和一个停止条件。

## 1. 项目定位与当前阶段

项目是独立运行的 AI 教学智能体。当前产品目标收敛为：教师提交一份 Markdown 教学材料，系统生成可人工审核、可本地导出的 Lab + Exam 教学包；Grading 作为 Exam 的内部配套评分规则，不作为独立产品线扩张。

当前已越过 Phase 1 Mock 底座和真实 SDK / 最小真实 LLM PoC。此前完成的 PPT、受控评分、内部实体、CLI、API、MCP、Agent 和多页前端能力继续保留，但不再同时作为当前 MVP 的交付目标。继续开发默认只收敛上述单一教学包闭环，具体顺序以进度地图为准。

## 2. 基本架构顺序

优先遵循：

```text
DSL → CLI → Mock → Workflow → Sandbox → MCP → Agent → Operation
```

Agent 只编排已经稳定的 CLI/API/MCP 工具，业务规则应留在 DSL、服务和工具契约中。

## 3. 当前范围与停止线

### 3.1 不再循环追加同义安全壳

`real-llm-request-send-attempt-gate-disabled` 是此前安装前/请求发送前安全建模的封口标记。除非用户明确给出新的具体风险、输入、输出和停止条件，否则不得新增同义的 `*-disabled`、`*-gate`、`*-executor`、`*-authorization`、`*-review-only`、`pre-install` 或 `request-send` 模块。

功能达到进度地图的停止线后，转向下一项核心业务，不继续堆同义 planning、展示页、验收壳或门禁。

### 3.2 默认开发范围

默认优先：Markdown 输入、Lab + Exam/Grading 生成、Schema 校验与归一化、Exam 候选人脱敏、`WAITING_REVIEW`、人工批准/退回、本地导出，以及支撑这条链路所需的最小本地状态、单一生成入口和单一审核入口。

默认只维护、不扩张：已有 PPT/PPTX、受控评分沙箱、本地实体与 import-preview/mock-import/import-dry-run、MCP、Agent、数据库 adapter 和其他前端页面。除非出现影响现有兼容性或安全边界的具体缺陷，否则不把这些能力列为下一步。

默认暂停：自动评分生产化、PPT 产品化、外部平台 API、平台 token/字段映射、`import-send` / `import-status` / 平台签收发布、MCP/Agent 新能力、VM/Notebook、生产部署、运营交付页、`operations-*` 页面、`delivery/` 新运营材料。只有用户明确恢复对应任务时才进入。

### 3.3 当前 MVP 停止线

当前 MVP 达到以下条件即停止扩张：

1. 一份 Markdown 可稳定生成相互引用正确的 Lab + Exam/Grading。
2. 产物通过 Schema/契约校验，并默认进入 `WAITING_REVIEW`。
3. 候选人预览不包含答案或内部 `gradingRef`。
4. 教师可在一条清晰流程中查看、批准或退回，并本地导出教学包。
5. Mock 正常路径和至少一个错误/回归路径可复现验证；真实 LLM 只对实际失败样本补修复。

达到该停止线后，只能在 PPT 产品化与自动评分产品化中选择一个作为下一阶段，且需要用户明确确认；不得并行恢复全部历史路线。

## 4. 不可退让的安全与数据规则

- AI 生成结果先产出 DSL，经过 Schema 校验，并默认进入 `WAITING_REVIEW`；人工审核前不得发布或自动改变审核结论。
- Exam 候选人端不得暴露标准答案或内部 `gradingRef`；日志、诊断和前端也不得展示密钥。
- API Key、Token、密码只能来自环境变量、配置或示例文件，不能写入代码或 Git。
- 不直接操作生产数据库，不删除云资源，不执行未知 Shell，不在无沙箱环境运行选手代码。
- 评分执行必须使用受控沙箱；评分证据可解释、可审计，不能把 evidence 直接等同于人工审核或发布。
- 不随意修改公共接口、不做无关的大范围重构、不删除用户已有逻辑。
- Prompt 放在 `prompts/` 或 `skills/`，业务代码引用文件，不把长 Prompt 散落在业务逻辑中。

## 5. 工程契约

- 优先维护 Lab / Exam / Grading DSL；PPT DSL 保持现有兼容性。生成、归一化和跨产物引用都要有 Schema 或契约校验。
- CLI 命令返回统一 JSON envelope；新增或修改命令要保持已有公共字段兼容。
- 真实 SDK、环境变量、client 构造或真实 LLM 请求必须显式 opt-in；默认 Mock 路径不因新增功能而改变。
- 真实生成仍必须保留 `WAITING_REVIEW`、失败诊断、审计上下文和不自动发布边界。

## 6. 每轮开发流程

开发前：

1. 读本文件。
2. 读 `docs/24_PROJECT_PROGRESS_MAP.md` 的当前结论、相关路线和对应停止线。
3. 读被修改模块的 API/Schema/README；只有需要架构背景时才查阅 `docs/AI_PLATFORM_CODEX_FULL_GUIDE.md` 或历史 Phase 文档。
4. 检查现有实现、测试和是否已有同义功能，判断任务属于 P0/P1/P2/P3 哪条路线。
5. 给出小计划，只修改任务需要的文件。

开发后：

1. 对可执行行为运行与风险匹配的测试；至少覆盖正常路径和错误/回归路径。只有触及 Schema、状态流转、CLI 或前端数据契约时，才强制补对应专项测试。
2. 行为、接口或路线变化时更新最近的 README、API/Schema 文档或进度地图；文档-only 修改不要求机械新增测试。
3. 检查 diff、密钥、审核状态、候选人脱敏和无沙箱执行边界。
4. 按第 7 节格式交付结果；不要在最终说明中重复整套规则。

## 7. 完成与输出格式

代码/功能任务完成至少应具备：实现、错误处理、可复现验证、必要测试、相关文档和清晰限制说明。新增模块还应有 README、输入/输出说明、命令示例、测试方式和限制说明。

完成开发后使用以下结构：

```text
本次完成：
- ...

修改文件：
- ...

如何验证：
- ...

测试结果：
- ...

风险与注意事项：
- ...

下一步建议：
- ...
```
